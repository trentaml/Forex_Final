<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.forex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SWComparison"></activity>
        <activity android:name=".AUComparison" />
        <activity android:name=".CAComparison" />
        <activity android:name=".NZComparison" />
        <activity android:name=".JPComparison" />
        <activity android:name=".UKComparison" />
        <activity android:name=".EUComparison" />
        <activity android:name=".USComparison" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>